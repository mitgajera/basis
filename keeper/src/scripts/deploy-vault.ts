// Phase 3: deploy vault script — deploys Anchor vault to devnet and saves program ID
// Stub for Phase 0 scaffold; implemented in Phase 3 (run via WSL)
console.log("Vault deployment is handled in anchor/ via WSL.");
console.log("See: cd anchor && anchor build && anchor deploy --provider.cluster devnet");
